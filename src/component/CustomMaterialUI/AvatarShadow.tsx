import styled from '@emotion/styled';
import { Avatar } from '@mui/material';

const AvatarShadow = styled(Avatar)({
    boxShadow: '0px 4px 10px rgba(0, 0, 0, 0.3)', // Customize the shadow
});

export default AvatarShadow;